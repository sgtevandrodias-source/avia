// Copie este arquivo para "secrets.ts" (que é ignorado pelo git) e cole a
// chave real, a mesma que foi configurada no Worker via `wrangler secret put API_KEY`.
export const API_KEY = 'coloque-a-chave-aqui';
